# Версия 6.3

**Дата релиза:** 28 марта 2026  
**Статус:** ✅ Актуальная

---

## 📋 Обзор

Оптимизация кэширования медиа: удалена таблица `user_cached_media`. Кэш теперь привязан только к источникам, без пользовательского уровня.

---

## 🔧 Изменения

| Файл | Изменения |
|------|-----------|
| `migrations/versions/dd03477f3d0e_remove_user_cached_media_table.py` | Удаление таблицы `user_cached_media` |
| `core/models/cache.py` | Удалена модель `UserCachedMedia` |
| `core/services/cleanup/media_cleanup.py` | Обновлена логика очистки |

---

## 🗄️ Миграции БД

**Migration ID:** `dd03477f3d0e`

### Удалённая таблица

```sql
-- Удалена таблица
DROP TABLE user_cached_media;
```

### Оставшиеся таблицы кэша

| Таблица | Назначение |
|---------|------------|
| `cached_media` | Кэш медиафайлов (привязка к источнику) |

---

## 📊 Обоснование

**Почему удалено:**
- `user_cached_media` дублировала информацию из `cached_media`
- Избыточность: один файл кэшировался для всех пользователей
- Упрощение: кэш на уровне источника, не пользователя

---

## 🧪 Тесты

- Добавлено тестов: 0 (изменение БД)
- Пройдено существующих: 99/104

---

## ⚠️ Известные проблемы

- Нет

---

## 📝 Заметки

Это оптимизация структуры БД без изменения функциональности.
